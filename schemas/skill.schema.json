{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "skill.schema.json",
  "title": "Skill",
  "description": "Metadata for a lazy-loaded LLM skill. This schema describes the required YAML frontmatter block at the top of every .skill.md file.",
  "type": "object",
  "required": ["id", "version", "status", "triggers"],
  "properties": {
    "id": {
      "type": "string",
      "description": "Unique skill identifier (slug format, matches filename)"
    },
    "version": { "type": "string" },
    "status": {
      "type": "string",
      "enum": ["draft", "active", "deprecated"]
    },
    "owner": {
      "type": "string",
      "description": "Agent or human responsible for this skill"
    },
    "triggers": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Natural language phrases that cause an agent to load this skill"
    },
    "outputs": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Types of artifacts this skill produces"
    },
    "quality_checks": {
      "type": "array",
      "items": { "type": "string" }
    }
  }
}
